# 测试报告 — <范围>

> `/test` 填表用模板。每次跑测试时把本文件复制到 `docs/test-reports/<YYYY-MM-DD-HHmm>-<范围>.md`。
> **不要**修改模板本身的内容 (除非是完善骨架本身)。每次运行的报告写在独立文件里。

## 元信息

| 字段 | 值 |
|------|----|
| 运行开始时间 | `<YYYY-MM-DD HH:mm>` (本地) |
| 目标 (`$ARGUMENTS`) | `<glob / 文件 / 模块>` |
| 执行者 | `/test` |
| Vitest 版本 | `<vitest --version 输出>` |
| 总耗时 | `<秒>` |
| 生成方 | `claude-code` |

## 执行汇总

| 指标 | 数量 |
|------|------|
| 总用例数 | `<n>` |
| ✅ 通过 | `<n>` |
| ❌ 失败 | `<n>` |
| ⏭️ 跳过 | `<n>` |

### 失败用例

> 全绿时整个小节可省略。

| `it()` 描述 | 源文件 | 关联规则 | 错误摘要 |
|--------------|--------|----------|----------|
| `<it() 名称原文>` | `workspace/src/.../Foo.tsx` | R2 | `Expected element to be disabled, but it was enabled` |

## 业务规则追溯矩阵

> 核心章节。每个被测源文件一个子块。规则来自该源文件的 `@rules` JSDoc — 不要改写。

### `workspace/src/features/list/components/SearchForm.tsx`

| 规则 ID | 规则原文 | 来源 | 对应 `it()` 描述 | 状态 |
|---------|----------|------|-------------------|------|
| R1 | 手机号输入后必须通过 PHONE_REG 校验 | `@rules` | `手机号格式不合法时应显示错误提示` | ✅ |
| R2 | 搜索按钮在所有字段为空时禁用 | `@rules` / PRD#搜索表单/规则2 | `所有字段为空时搜索按钮应禁用` | ❌ |
| R3 | 重置后自动触发一次查询 | `@task#task-003` | _(缺测试)_ | ⚠️ 未覆盖 |

**规则覆盖率**: `2/3 = 67%`

> 每个目标源文件都重复上面这块。

## 本轮变更

> 相比上一份报告或 git 基线的差异。每项一句话理由。

### 新增

- `tests/features/list/components/SearchForm.test.tsx` — 为 R1–R3 新建测试文件 (此前未覆盖)

### 修改

- `tests/features/list/api/listApi.test.ts` — 更新 mock 响应形状, 对齐 `@/types/api` 中最新的 `paths['/api/users/search']`

### 删除

- _(无)_

## 未解决问题与建议

### 失败分诊

| 失败 | 类别 | 处理建议 |
|------|------|----------|
| R2 `所有字段为空时搜索按钮应禁用` | 源码 bug | 源码未做「全空」判断, 需在 `SearchForm.tsx` 中补。按 `/test` 政策停在此处, 不自动改源码。 |

### 未覆盖的规则

- R3 (`SearchForm.tsx`) — 需补一个 `it()`, 依赖 `useSearch` hook 的 mock fixture。

### 待人工跟进

- [ ] 确认 R2 是源码 bug 还是规则措辞偏差 (PRD 写「禁用」, 代码看起来是「启用并校验」), 据此修 PRD 或修源码。
- [ ] 下次 `/test` 在 fixture 就绪后补 R3 测试。

### 备注

> 自由记录。任何异常情况都可写在这里: flaky 测试、环境怪象、影响结果的依赖升级等。
