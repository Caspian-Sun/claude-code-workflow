# Test Report — <scope>

> Template for `/test` to fill in. Copy this file to `docs/test-reports/<YYYY-MM-DD-HHmm>-<scope>.md` for each run.
> Do **not** edit this template's content other than to improve the skeleton. Per-run reports go in separate files.

## Metadata

| Field | Value |
|-------|-------|
| Run started at | `<YYYY-MM-DD HH:mm>` (local) |
| Target (`$ARGUMENTS`) | `<glob / file / module>` |
| Executor | `/test` |
| Vitest version | `<vitest --version output>` |
| Total duration | `<seconds>` |
| Generated by | `claude-code` |

## Execution Summary

| Metric | Count |
|--------|-------|
| Total cases | `<n>` |
| ✅ Passed | `<n>` |
| ❌ Failed | `<n>` |
| ⏭️ Skipped | `<n>` |

### Failing Cases

> Omit this subsection entirely if all green.

| `it()` Description | Source File | Linked Rule | Error Excerpt |
|---------------------|-------------|-------------|---------------|
| `<verbatim it() name>` | `workspace/src/.../Foo.tsx` | R2 | `Expected element to be disabled, but it was enabled` |

## Business Rule Traceability Matrix

> The core section. One sub-block per source file under test. Rules come from each source file's `@rules` JSDoc — do not paraphrase.

### `workspace/src/features/list/components/SearchForm.tsx`

| Rule ID | Rule Text | Source | `it()` Description | Status |
|---------|-----------|--------|---------------------|--------|
| R1 | Phone number must pass PHONE_REG validation after input | `@rules` | `should show error when phone number format is invalid` | ✅ |
| R2 | Search button disabled when all fields empty | `@rules` / PRD#search-form/rule2 | `should disable search button when all fields are empty` | ❌ |
| R3 | After reset, automatically trigger one query | `@task#task-003` | _(missing test)_ | ⚠️ Uncovered |

**Rule coverage**: `2/3 = 67%`

> Repeat the per-file block above for every targeted source file.

## Changes This Round

> Diff vs. the previous report or git baseline. One-line reason per change.

### Added

- `tests/features/list/components/SearchForm.test.tsx` — new test file for R1–R3 (was previously uncovered)

### Modified

- `tests/features/list/api/listApi.test.ts` — updated mock response shape to match latest `paths['/api/users/search']` from `@/types/api`

### Removed

- _(none)_

## Open Issues & Recommendations

### Failures Triaged

| Failure | Category | Action |
|---------|----------|--------|
| R2 `should disable search button when all fields are empty` | Source code bug | Source does not check the all-empty case; needs fix in `SearchForm.tsx`. Stopped here per `/test` policy — do not modify source autonomously. |

### Uncovered Rules

- R3 (`SearchForm.tsx`) — needs an `it()` added; depends on integrating mock for `useSearch` hook.

### Follow-ups for Humans

- [ ] Confirm whether R2 is a source bug or a rule miswording (PRD says "disabled", code suggests "enabled with validation"); update PRD or source accordingly.
- [ ] Add the missing R3 test in next `/test` run after fixture is ready.

### Notes

> Free-form. Mention anything unusual: flaky tests, environment quirks, dependency upgrades that affected results, etc.
